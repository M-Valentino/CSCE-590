  In order to run this project, first import the the entire folder (including the CSVs and Jupyter notebooks) as a Java project into a Java IDE such as Eclipse.
  Then run the Java Program. After that, The Jupyter notebooks can be run. CitiesRecomenderPart1 should be run first, and then CitiesRecomenderPart2 should be run
  after that.
  
